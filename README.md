# M9PRog - Webshop

WordPress webshop met een eigen theme, gemaakt voor M9PROG.

## Starten

1. Kopieer `.env.example` naar `.env` en vul de wachtwoorden in
2. `docker compose up -d`
3. WordPress: http://localhost:8080
4. phpMyAdmin: http://localhost:8082

Het theme staat in `webshop-theme/`.
